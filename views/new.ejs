<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add New Listing | WanderLust</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #fafafa;
      color: #222;
      margin: 0;
      padding: 0;
    }

    .header {
      background-color: white;
      padding: 20px 40px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-size: 24px;
      font-weight: bold;
      color: #FF385C;
    }

    .container {
      max-width: 600px;
      margin: 50px auto;
      background: white;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      padding: 40px;
    }

    h2 {
      text-align: center;
      color: #FF385C;
      margin-bottom: 30px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    label {
      font-weight: 600;
    }

    input, textarea {
      padding: 10px 14px;
      border-radius: 8px;
      border: 1px solid #ddd;
      font-size: 15px;
      transition: all 0.2s ease-in-out;
    }

    input:focus, textarea:focus {
      outline: none;
      border-color: #FF385C;
      box-shadow: 0 0 3px rgba(255, 56, 92, 0.4);
    }

    textarea {
      resize: none;
      height: 100px;
    }

    button {
      background-color: #FF385C;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 12px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s ease-in-out;
    }

    button:hover {
      background-color: #e02e50;
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 20px;
      color: #555;
      text-decoration: none;
    }

    .back-link:hover {
      text-decoration: underline;
    }
    select {
      padding: 10px 14px;
      border-radius: 8px;
      border: 1px solid #ddd;
      font-size: 15px;
      font-family: 'Poppins', sans-serif;
      background-color: #fff;
      cursor: pointer;
      transition: all 0.2s ease-in-out;
    }

    select:focus {
      outline: none;
      border-color: #FF385C;
      box-shadow: 0 0 3px rgba(255, 56, 92, 0.4);
    }
    .file-input {
      padding: 8px;
      border-radius: 8px;
      border: 1px solid #ddd;
      font-size: 14px;
      background-color: #fff;
      cursor: pointer;
    }

    .file-input::file-selector-button {
      background-color: #FF385C;
      color: white;
      border: none;
      border-radius: 6px;
      padding: 8px 14px;
      margin-right: 12px;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.2s ease-in-out;
    }

    .file-input::file-selector-button:hover {
      background-color: #e02e50;
    }
  </style>
</head>
<body>

  <header class="header">
    <div class="logo">WanderLust</div>
    <a href="/listing" class="back-link">← Back to Listings</a>
  </header>

  <div class="container">
    <h2>Add New Listing</h2>
    <% if (typeof error !== "undefined") { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <strong>Error:</strong> <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
    <form action="/listing" method="POST" class="needs-validation" novalidate enctype="multipart/form-data">
      <label for="title">Title</label>
      <input type="text" id="title" name="title" class="form-control" required placeholder="e.g. Cozy Cottage in Manali">
      <div class="invalid-feedback">
        Please enter a title.
      </div>

      <label for="description">Description</label>
      <textarea id="description" name="description" class="form-control" required placeholder="Short description..."></textarea>
      <div class="invalid-feedback">
        Please enter a description.
      </div>
      <label for="img">Image Upload</label>
      <input type="file" id="img" name="listing[image]" class="file-input">
      <label for="price">Price (₹)</label>
      <input type="number" id="price" name="price" class="form-control" min="1" required placeholder="e.g. 2500">
      <div class="invalid-feedback">
        Please enter a price.
      </div>
      <label for="filter">Listing Type</label>
      <select name="filter" required>
          <option value="">Select Type</option>
          <option value="beachfront">Beachfront</option>
          <option value="lakefront">Lakefront</option>
          <option value="tinyhome">Tiny Home</option>
          <option value="treehouse">Tree House</option>
          <option value="countryside">Countryside</option>
          <option value="historical">Historical</option>
          <option value="tropical">Tropical</option>
          <option value="lux">Luxe</option>
          <option value="vineyards">Vineyards</option>
      </select>

      <label for="location">Location</label>
      <input type="text" id="location" name="location" class="form-control" required placeholder="e.g. Mumbai">
      <div class="invalid-feedback">
        Please enter a location.
      </div>
      <label for="country">Country</label>
      <input type="text" id="country" name="country" class="form-control" required placeholder="e.g. India">
      <div class="invalid-feedback">
        Please enter a contry.
      </div>
      <button type="submit">Add Listing</button>
    </form>

    <a href="/listing" class="back-link">← Back to Listings</a>
  </div>
  <script>
  (() => {
    'use strict';

    const forms = document.querySelectorAll('.needs-validation');

    Array.from(forms).forEach(form => {
      form.addEventListener('submit', event => {
        if (!form.checkValidity()) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  })();
  </script>

</body>
</html>
